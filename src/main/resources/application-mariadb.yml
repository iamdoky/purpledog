---

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mysql://localhost:3306/purpledog
    username: root
    password: mariadb
  jpa:
    hibernate:
      ddl-auto: create

...